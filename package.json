{
  "name": "logisticshandover",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Balter Brewing — Logistics Daily Handover (password-gated Cloudflare Worker + static site)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
